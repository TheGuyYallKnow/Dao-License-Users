# Dao-License-Users

Owner-controlled public user license source for Library Bridge.

The plugin reads `users.json` from the `main` branch. Keep `schemaVersion` at `1`, increment `revision` whenever access changes, and keep `users` as a sorted array of positive numeric Roblox user IDs.